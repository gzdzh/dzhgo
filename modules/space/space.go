package demo

import (
	_ "github.com/gzdzh/dzhgo/modules/space/controller"
	_ "github.com/gzdzh/dzhgo/modules/space/middleware"
)

func init() {
	// var (
	// 	ctx = gctx.GetInitCtx()
	// )
	// g.Log().Debug(ctx, "module space init start ...")
	// g.Log().Debug(ctx, "module space init finished ...")
}
